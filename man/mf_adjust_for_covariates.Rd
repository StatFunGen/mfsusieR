% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_covariates.R
\name{mf_adjust_for_covariates}
\alias{mf_adjust_for_covariates}
\title{Adjust a functional response for covariate effects}
\usage{
mf_adjust_for_covariates(
  Y,
  Z,
  X = NULL,
  method = c("wavelet_eb", "ols"),
  wavelet_filter_number = 10L,
  wavelet_family = "DaubLeAsymm",
  max_iter = 100L,
  tol = 0.001,
  null_prior_weight = 10,
  init_pi0_w = 1,
  control_mixsqp = list(verbose = FALSE, eps = 1e-06, numiter.em = 4L),
  grid_mult = sqrt(2),
  low_count_filter = 0,
  quantile_norm = FALSE
)
}
\arguments{
\item{Y}{\verb{n x T} numeric matrix; the functional response. \code{T}
must be a power of two for \code{method = "wavelet_eb"}.}

\item{Z}{\verb{n x K} numeric matrix; covariates to adjust away.}

\item{X}{optional \verb{n x p} numeric matrix; the genotype matrix
to FWL-residualize. Default \code{NULL}.}

\item{method}{\code{"wavelet_eb"} (default) or \code{"ols"}.}

\item{wavelet_filter_number}{integer, the \code{filter.number}
argument to \code{wd}. Default \code{1L}.}

\item{wavelet_family}{character, the \code{family} argument to
\code{wd}. Default \code{"DaubExPhase"}.}

\item{max_iter}{integer, maximum outer iterations for the
wavelet-EB path. Default \code{100}.}

\item{tol}{numeric, convergence tolerance on the per-scale
prior weights \verb{||pi - pi_prev|| / log(K) < tol}.
Default \code{1e-3}.}

\item{null_prior_weight}{numeric, penalty on the null component
(passed through to the EB prior init). Default \code{10}.}

\item{init_pi0_w}{numeric, starting null-component mass for
mixsqp. Default \code{1}.}

\item{control_mixsqp}{optional named list of \code{mixsqp} control
args. Default \code{list(verbose = FALSE, eps = 1e-6, numiter.em = 4L)}.}

\item{grid_mult}{numeric, multiplier for the ash mixture grid.
Default \code{sqrt(2)}.}

\item{low_count_filter}{non-negative numeric. Wavelet-domain
columns of \code{Y} with \verb{median(|column|) <= low_count_filter}
are flagged uninformative (\code{Bhat = 0}, \code{Shat = 1}) at every
outer iteration. Default \code{0}. Useful for sparse-coverage
responses where many wavelet columns carry negligible
signal.}

\item{quantile_norm}{logical. When \code{TRUE}, applies a
column-wise rank-based normal quantile transform to the
wavelet-domain response before the EB regression loop. The
returned \code{Y_adjusted} is on the original (un-transformed)
position scale so downstream \code{fsusie(Y_adjusted, X)} calls
operate on the same units as the input \code{Y}. Default
\code{FALSE}.}
}
\value{
A named list with:
\describe{
\item{\code{Y_adjusted}}{\verb{n x T} matrix, the residualized response.}
\item{\code{X_adjusted}}{\verb{n x p} matrix when \code{X} was supplied,
else \code{NULL}.}
\item{\code{fitted_func}}{\verb{K x T} matrix of fitted covariate
effects.}
\item{\code{sigma2}}{numeric, residual variance estimate.}
\item{\code{niter}}{integer, number of outer iterations
(wavelet-EB only).}
\item{\code{converged}}{logical, convergence status (wavelet-EB
only).}
\item{\code{method}}{character, the method used.}
}
}
\description{
Returns \code{Y_adjusted = Y - Z \%*\% fitted_func} where \code{fitted_func}
is a \verb{K x T} matrix of per-position covariate effects fit by one
of two methods:
}
\details{
\itemize{
\item \code{"wavelet_eb"} (default): wavelet-domain empirical-Bayes
regression with a per-scale mixture-of-normals prior. Suitable
when the covariate effects are smooth functions of position.
\item \code{"ols"}: closed-form Frisch-Waugh-Lovell residualization. The
no-shrinkage limit of the wavelet-EB path; suitable when the
covariate effects can vary arbitrarily across positions.
}

When \code{X} is supplied, the function also returns
\verb{X_adjusted = (I - Z (Z'Z)^\{-1\} Z') X} to handle covariate-
genotype correlation. Upstream covariate-adjustment workflows
typically do not residualize \code{X}; the option is provided for
downstream fine-mapping users who need it.
}
\references{
Manuscript: methods/online_method.tex eq:wavelet_residualization
(covariate adjustment in the wavelet domain).
}
