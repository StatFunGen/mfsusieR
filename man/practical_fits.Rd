% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{practical_fits}
\alias{practical_fits}
\title{Fitted mfsusie objects under five preprocessing configurations}
\format{
A list with components
\describe{
\item{\code{n}, \code{p}, \code{M}, \code{T_m}, \code{signal_snp}}{simulation metadata.}
\item{\code{cell_types}}{character vector of length \code{M} naming the
two simulated cell types.}
\item{\code{default}}{\code{mfsusie} fit with all defaults (Wakefield
Normal marginal Bayes factor).}
\item{\code{johnson}}{\code{mfsusie} fit with
\code{small_sample_correction = TRUE} (Johnson-t marginal).}
\item{\code{lowcount}}{\code{mfsusie} fit with \code{low_count_filter = 0.02}.}
\item{\code{qn}}{\code{mfsusie} fit with \code{quantile_norm = TRUE}.}
\item{\code{combined}}{\code{mfsusie} fit with all three corrections
enabled.}
}

Each fit is stripped of \code{residuals}, \code{XtY}, \code{tracking}, and any
raw \code{X}/\code{Y} matrices to keep the package size small and avoid
shipping individual-level data.
}
\source{
Simulated. See \code{data-raw/make_practical_dataset.R}.
}
\usage{
practical_fits
}
\description{
Five fits on the same simulated dataset (\code{n = 40}, \code{p = 25},
\code{M = 2}, \code{T_m = 32}, signal SNP at index 7), one per
configuration: default (Wakefield Normal marginal), small-sample
Student-t correction, low-count column filtering,
wavelet-domain quantile normalization, and all three
corrections combined. The simulated dataset reproduces the
shape of a small-cohort chromatin-accessibility QTL panel
(sparse coverage in some cell types, heavy-tailed wavelet
coefficients in others). The underlying genotype and
read-count data are not redistributable; this object ships
fitted-model summaries only, no individual-level inputs.
}
\examples{
\donttest{
data(practical_fits)
practical_fits$default$pip[practical_fits$signal_snp]
}
}
\keyword{datasets}
