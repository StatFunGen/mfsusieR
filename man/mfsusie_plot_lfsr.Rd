% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfsusie_plot.R
\name{mfsusie_plot_lfsr}
\alias{mfsusie_plot_lfsr}
\title{Per-CS local false sign rate bubble grid}
\usage{
mfsusie_plot_lfsr(
  fit,
  lfsr_threshold = 0.01,
  truth = NULL,
  cex_max = 6,
  add_legend = TRUE,
  smooth_method = NULL,
  save = NULL,
  ...
)
}
\arguments{
\item{fit}{an \code{mfsusie} / \code{fsusie()} fit object that has
been post-processed with \code{mf_post_smooth(method = "HMM")}
(so \code{fit$lfsr_curves} is populated).}

\item{lfsr_threshold}{numeric, color cutoff. Positions with
lfsr at or below \code{lfsr_threshold} use the CS color; the
rest are grey. Default \code{0.01}.}

\item{truth}{optional ground-truth mask for simulated data.
For \code{M = 1}: a length-\code{T_1} boolean vector OR a length-K
list of length-\code{T_1} boolean vectors (one per CS). For
\code{M > 1}: a length-M list, each entry a length-\code{T_m}
boolean vector or list-of-CS-vectors.}

\item{cex_max}{numeric, upper clamp on \code{-log10(lfsr)} for
the dot size. Default \code{6}.}

\item{save}{optional file path. When non-NULL the bubble grid
is written to the file at a size proportional to the number
of outcomes and credible sets. The graphics device is
selected from the file extension: \code{.pdf}, \code{.png},
\code{.jpg}/\code{.jpeg}, or \code{.svg}.}

\item{...}{reserved.}
}
\value{
Called for side effect; returns \code{invisible(NULL)}.
}
\description{
Visualize the per-position local false sign rate (lfsr)
produced by \code{mf_post_smooth(method = "HMM")} as a bubble
grid: rows are credible sets, columns are positions, dot
size is \code{-log10(lfsr)} clamped to \verb{[0, cex_max]}. Color
marks positions where lfsr is at or below \code{lfsr_threshold}.
For simulated data where the truly affected positions are
known, pass \code{truth} and dots are recolored by the
ground-truth mask.
}
\details{
Companion to \code{mfsusie_plot()} for HMM-smoothed fits. The
function handles single-outcome (\code{M = 1}) and multi-outcome
(\code{M > 1}) fits transparently and takes no \code{m} argument; for
\code{M > 1} it tiles one bubble panel per outcome using the same
layout policy as \code{mfsusie_plot()}.
}
