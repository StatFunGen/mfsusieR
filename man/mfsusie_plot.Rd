% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfsusie_plot.R
\name{mfsusie_plot}
\alias{mfsusie_plot}
\title{Plot an mfsusie() / fsusie() fit}
\usage{
mfsusie_plot(
  fit,
  m = NULL,
  pos = NULL,
  effect_style = c("band", "errorbar"),
  facet_cs = c("auto", "stack", "overlay"),
  show_grid_dots = FALSE,
  show_lfsr_curve = TRUE,
  lfsr_source = c("smoother", "lfsr", "clfsr"),
  show_affected_region = TRUE,
  lfsr_threshold = 0.01,
  lwd = 2,
  add_legend = TRUE,
  truth = NULL,
  effect_variables = NULL,
  smooth_method = NULL,
  save = NULL,
  ...
)
}
\arguments{
\item{fit}{a fit returned by \code{mfsusie()} or \code{fsusie()}.}

\item{m}{optional integer index. When supplied, plot only that
outcome's effect panel (no PIP). Default \code{NULL}: full layout.}

\item{pos}{optional length-p vector for the PIP x-axis.}

\item{effect_style}{\code{"band"} (default) or \code{"errorbar"}. Style
of the effect panel.}

\item{facet_cs}{\code{"auto"} (default), \code{"stack"}, or \code{"overlay"}.
Layout of multiple credible sets within an effect panel.}

\item{show_grid_dots}{logical, draw circles at each post-remap
grid point on top of the curves. Default \code{FALSE}.}

\item{show_lfsr_curve}{logical, overlay an lfsr curve on a
secondary axis. Only honored in \code{effect_style = "band"}.
Default \code{TRUE}. The solid line is the effect (left axis), the
dashed line is the lfsr (right axis, 0 to 1). The lfsr source
is selected by \code{lfsr_source} below. For a dedicated per-CS
lfsr view see \code{mfsusie_plot_lfsr()}.}

\item{lfsr_source}{which lfsr to overlay, one of
\code{"smoother"}, \code{"lfsr"}, \code{"clfsr"}. Default \code{"smoother"}.
\itemize{
\item \code{"smoother"}: reads \code{fit$smoothed[[method]]$lfsr_curves[[m]][[l]]},
a length-\code{T_m} per-position vector produced by the smoother
kernel itself.
\item \code{"lfsr"}: alpha-weighted aggregation of the per-variant
conditional lfsr matrix, \code{alpha[l, ] \%*\% clfsr_matrix[l, m]},
truncated to length \code{T_m}. Computed from \code{fit$mu} /
\code{fit$mu2} so it is available even on a fit that has not been
post-smoothed.
\item \code{"clfsr"}: same alpha-weighted aggregation as \code{"lfsr"}. The
unaggregated \verb{p x T_basis[m]} conditional matrix
\verb{pnorm(-|mu[l, m, j, t]| / sd[l, m, j, t])} is exposed via
\code{compute_clfsr_matrix(fit, l, m)} for code that needs the
full per-variant view.
}}

\item{show_affected_region}{logical, mark contiguous segments
where each CS's credible band excludes zero with thick bars
at the bottom of the effect panel (band mode) or with black
dots on the zero line (errorbar mode). Default \code{TRUE}.}

\item{lfsr_threshold}{numeric, dashed reference line on the lfsr
secondary axis. Default \code{0.01}.}

\item{lwd}{numeric, curve line width. Default \code{1.5}.}

\item{add_legend}{logical, show CS legend on each panel.
Default \code{TRUE}.}

\item{truth}{optional ground-truth overlay for the \emph{effect-target}
panels (positions along the function where the true effect is
non-zero). For \code{M = 1}: a length-\code{T_1} numeric vector
(replicated across all CS panels) or a length-\code{K} list of
length-\code{T_1} vectors (per-CS truth). For \code{M > 1}: a length-\code{M}
list, each entry one of those two shapes (or \code{NULL} to skip an
outcome). Truth values are overlaid as dark-grey dots in each
effect panel. Default \code{NULL} (no overlay).}

\item{effect_variables}{optional indicator of the \emph{true-effect
variables} (column indices of \code{X} that carry a non-zero true
effect). When non-NULL, the PIP panel draws a red bold open
circle around each indicated variable's PIP dot, mirroring
susieR::susie_plot()'s \code{b} argument. Accepts: integer index
vector (1-based), length-\code{p} logical (\code{TRUE} = true effect),
or length-\code{p} numeric (non-zero = true effect). Default
\code{NULL}.}

\item{save}{optional file path. When non-NULL the plot is
written to the file at the dimensions returned by
\code{mfsusie_plot_dimensions()}. The graphics device is selected
from the file extension: \code{.pdf}, \code{.png}, \code{.jpg}/\code{.jpeg}, or
\code{.svg}. Default \code{NULL} (draw to the current device).}

\item{...}{reserved.}
}
\value{
Called for side effect; returns \code{invisible(NULL)}.
}
\description{
One unified plot for both single-outcome (\code{fsusie()}) and
multi-outcome (\code{mfsusie()}) fits. When \code{M = 1} the layout is a
single 2-panel column (PIP on top, effect curves on bottom). When
\code{M > 1} the layout tiles per-outcome panels on a dense grid with
the PIP plot in the top-left slot.
}
\details{
Effect curves come from \code{fit$effect_curves} when post-processed
smoothing has been applied (see \code{mf_post_smooth()}); otherwise
from the wavelet-domain inverse via \code{coef()}. With
\code{effect_style = "band"} (default) credible bands are drawn as
transparent ribbons; with \code{effect_style = "errorbar"} each
position gets a dot at the mean and a vertical bar to the
credible band (errorbar mode requires post-smoothed credible
bands and errors otherwise).

Multiple credible sets can be drawn overlaid (\code{facet_cs = "overlay"}) or in stacked rows (\code{facet_cs = "stack"}); the
default \code{"auto"} picks stack when there are at least 3 CSes or
when their affected-region masks are pairwise disjoint.

After \code{mf_post_smooth(method = "HMM")} the per-position lfsr
curves are overlaid on a secondary axis (band mode) with a
threshold line at \code{lfsr_threshold} (default \code{0.01}). For the
per-CS lfsr bubble grid see \code{mfsusie_plot_lfsr()}.
}
