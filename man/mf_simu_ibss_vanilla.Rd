% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{mf_simu_ibss_vanilla}
\alias{mf_simu_ibss_vanilla}
\title{Sample a smooth random function from the vanilla IBSS prior}
\usage{
mf_simu_ibss_vanilla(lev_res = 8L, length_grid = 10L, pi0 = 0.85)
}
\arguments{
\item{lev_res}{integer >= 2. Resolution: the simulated
function lives on a length-\code{2^lev_res} grid.}

\item{length_grid}{integer >= 2. Number of mixture
components (including the null at component 1).}

\item{pi0}{numeric in \verb{[0, 1]}. Prior probability that any
given wavelet detail coefficient is exactly zero.
Default 0.85.}
}
\value{
A list with components:
\describe{
\item{\code{sim_func}}{length-\code{2^lev_res} numeric vector,
the simulated function in position space.}
\item{\code{true_coef}}{wavelet detail coefficients used to
construct \code{sim_func}.}
\item{\code{true_g}}{the \code{ashr::normalmix} object
describing the shared mixture prior.}
\item{\code{emp_pi0}}{empirical proportion of detail
coefficients drawn from the null component (the
Monte Carlo realization of \code{pi0}).}
}
}
\description{
Simpler companion to \code{mf_simu_ibss_per_level()}: draws all
wavelet detail coefficients from a SINGLE shared
mixture-of-normals (no per-scale variation). The mixture has
one null component (\code{sd = 0}) with mass \code{pi0}; the remaining
mass is split uniformly across \code{length_grid - 1} non-null
components whose standard deviations are drawn from a
0-anchored cumsum of chi-squared(1) variates.
}
\details{
Useful for quick simulation when you don't need per-scale
control over the prior. The per-scale variant
(\code{mf_simu_ibss_per_level}) better mirrors the prior fSuSiE
actually infers.
}
\examples{
set.seed(1L)
f <- mf_simu_ibss_vanilla(lev_res = 7L)
length(f$sim_func)
f$emp_pi0

}
