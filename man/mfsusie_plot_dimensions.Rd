% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfsusie_plot.R
\name{mfsusie_plot_dimensions}
\alias{mfsusie_plot_dimensions}
\title{Recommended figure dimensions for \code{mfsusie_plot()}}
\usage{
mfsusie_plot_dimensions(
  fit,
  m = NULL,
  facet_cs = c("auto", "stack", "overlay"),
  smooth_method = NULL,
  min_pip_height = 2.2,
  min_cell_height = 2,
  width = 8
)
}
\arguments{
\item{fit}{a fit returned by \code{mfsusie()} or \code{fsusie()}.}

\item{m}{optional integer index. When supplied, recommend
dimensions for the focused single-outcome view.}

\item{facet_cs}{\code{"auto"}, \code{"stack"}, or \code{"overlay"}. Default
\code{"auto"} matches \code{mfsusie_plot()}'s default.}

\item{smooth_method}{optional smoother name to resolve
\code{facet_cs = "auto"} against the smoothed credible bands.}

\item{min_pip_height}{minimum height (in) for the PIP cell.}

\item{min_cell_height}{minimum height (in) for each effect
cell.}

\item{width}{figure width in inches. Default \code{8}.}
}
\value{
a list with components \code{width}, \code{height} (numeric,
inches) and \code{n_cells} (integer, total panel count).
}
\description{
Returns a width and height (in inches) sized to the number of
panels \code{mfsusie_plot()} will draw on the supplied fit. The
recommendation accounts for the PIP cell, the per-outcome
effect cells, and the per-CS sub-cells when \code{facet_cs = "stack"} is used with \code{K >= 2} credible sets. Useful for
setting \code{fig.width} / \code{fig.height} in a knitr chunk so each
panel has enough vertical space to render legibly.
}
\details{
Heuristics: \code{min_pip_height} (default 1.6 in) for the PIP
cell, \code{min_cell_height} (default 1.5 in) for each effect
cell. The PIP cell carries 1.5x the height weight of an
effect cell. The recommended figure width defaults to
\code{width = 8} inches.
}
\examples{
\donttest{
set.seed(1L)
n <- 100; p <- 20
X <- matrix(rnorm(n * p), n)
Y <- list(matrix(rnorm(n * 32), n), matrix(rnorm(n * 32), n))
fit <- mfsusie(X, Y, L = 4, verbose = FALSE)
fit_s <- mf_post_smooth(fit)
mfsusie_plot_dimensions(fit_s, facet_cs = "stack")
}
}
