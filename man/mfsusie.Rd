% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfsusie.R
\name{mfsusie}
\alias{mfsusie}
\title{Multi-functional, multi-outcome SuSiE}
\usage{
mfsusie(
  X,
  Y,
  pos = NULL,
  L = 20,
  prior_variance_grid = NULL,
  prior_variance_scope = c("per_outcome", "per_scale"),
  null_prior_init = 0,
  cross_outcome_prior = NULL,
  prior_weights = NULL,
  residual_variance = NULL,
  residual_variance_scope = c("per_outcome", "per_scale"),
  standardize = TRUE,
  intercept = TRUE,
  max_iter = 50,
  tol = 1e-04,
  coverage = 0.95,
  min_abs_corr = 0.5,
  L_greedy = 5,
  greedy_lbf_cutoff = 0.1,
  estimate_prior_variance = TRUE,
  convergence_method = c("pip", "elbo"),
  pip_stall_window = 5L,
  estimate_residual_variance = TRUE,
  verbose = TRUE,
  track_fit = FALSE,
  max_padded_log2 = 10,
  wavelet_basis_order = 10,
  wavelet_family = "DaubLeAsymm",
  wavelet_magnitude_cutoff = 0,
  wavelet_qnorm = TRUE,
  control_mixsqp = NULL,
  mixture_null_weight = 0.05,
  mixsqp_alpha_eps = 1e-06,
  model_init = NULL,
  small_sample_correction = FALSE,
  attach_smoothing_inputs = TRUE
)
}
\arguments{
\item{X}{numeric matrix \verb{n x p} of covariates (e.g., genotype dosages).}

\item{Y}{list of length \code{M}; each element a numeric matrix \verb{n x T_m}
(or a length-n vector when \code{T_m = 1}).}

\item{pos}{optional list of length \code{M}; each element a numeric vector
of length \code{T_m} recording the sampling positions for that
outcome. Defaults to \code{seq_len(T_m)} per outcome.}

\item{L}{integer, maximum number of single effects to fit.
Default \code{20}. With the default \code{L_greedy = 5}, the IBSS
workhorse grows the effect count from 5 toward this cap in
steps of 5 until the fit saturates (\code{min(lbf) < greedy_lbf_cutoff}); set \code{L_greedy = NULL} to fit the full
\code{L} effects in one round.}

\item{prior_variance_grid}{optional length-K vector of mixture
variances for the scale-mixture-of-normals prior. When \code{NULL},
the data-driven path
(\code{compute_marginal_bhat_shat} + \code{ash}) selects the
grid per outcome.}

\item{prior_variance_scope}{\code{"per_outcome"} (default) or
\code{"per_scale"}. Controls whether the mixture-weight matrix
\code{pi_V[[m]]} collapses across scales (one weight vector per
outcome) or keeps a separate row per wavelet scale. The
per-outcome scope is the default because it is roughly
\code{S_m}-fold cheaper per IBSS iter (M mixsqp calls per iter
instead of M*S_m); switch to \code{"per_scale"} only when you
need scale-specific mixture weights for power on shape-
varying signals.}

\item{null_prior_init}{numeric in \verb{[0, 1]}. Initial mass on the
null component of the scale-mixture prior on \code{b_l}. Used
directly as \code{pi[null]} at IBSS iter 1; the EM M-step
overwrites it from the data within a few iterations, so the
final fit is essentially insensitive to this value. Default
\code{0} — no prior null bias at start, signalling "the data
should decide."}

\item{cross_outcome_prior}{optional object that controls how the
per-outcome log-Bayes factors are combined into the joint
log-Bayes factor used by the SER step. Each IBSS effect first
computes a length-\code{p} log-BF per outcome \code{m} (one entry per
variable, summed over wavelet positions); these are then
reduced to a single length-\code{p} joint log-BF before the
posterior \code{alpha} is taken as a softmax over variables.
Defaults to outcome independence
(\code{cross_outcome_prior_independent()}), under which the joint
log-BF is the elementwise sum of the per-outcome log-BFs;
equivalently, the joint BF is the product of per-outcome BFs.
The argument is an extension point for non-independence
combiners (e.g. modality-covariance priors) registered as S3
methods on \code{combine_outcome_lbfs}; only the independence
combiner is shipped, so most users leave this \code{NULL}.}

\item{prior_weights}{optional length-p numeric vector, the
variable-selection prior. Defaults to uniform \code{1/p}.}

\item{residual_variance}{optional list of length \code{M}, initial
residual variance per outcome. Defaults to the per-outcome
sample variance.}

\item{residual_variance_scope}{\code{"per_outcome"} (default) or
\code{"per_scale"}. Controls the sigma2 update shape: a single
scalar per outcome (default) or one scalar per wavelet scale
per outcome.}

\item{standardize}{logical, scale \code{X} columns to unit variance.}

\item{intercept}{logical, center \code{X} columns to mean zero.}

\item{max_iter}{integer, maximum IBSS iterations. Default \code{50}.}

\item{tol}{numeric, ELBO change tolerance for convergence.}

\item{coverage}{numeric in (0, 1), credible-set coverage.}

\item{min_abs_corr}{numeric, minimum variable-to-variable correlation
inside a credible set (CS purity threshold).}

\item{L_greedy}{integer or \code{NULL}. When non-\code{NULL} (default \code{5}),
run susieR's greedy outer loop, growing the effect count from
\code{L_greedy} up to \code{L} in linear steps until the fit saturates
(\code{min(lbf) < greedy_lbf_cutoff}). Set \code{NULL} to fit the full
\code{L} effects in one round.}

\item{greedy_lbf_cutoff}{numeric saturation threshold for the
greedy outer loop. Default 0.1.}

\item{estimate_prior_variance}{logical. When \code{TRUE} (default),
run the per-effect empirical-Bayes update of the mixture
weights \code{pi_V[[m]]} per (outcome, scale) using the \code{mixsqp}
solver. When \code{FALSE}, hold the prior fixed at the initial
\code{prior_variance_grid} / \code{null_prior_init}; useful when
collapsing \code{mfsusie()} to \code{susieR::susie()} for sanity
checks.}

\item{convergence_method}{one of \code{"pip"} (default) or
\code{"elbo"}. Selects the IBSS convergence criterion.
\code{"pip"} stops when \code{max(abs(prev_alpha - alpha))} falls
below \code{tol} (PIP-difference convergence) with stall
detection -- the default because alpha is robust to the
small per-iteration ELBO oscillations that arise from
mixsqp's approximate M-step (Generalized EM residual:
\code{ELBO(t+1) >= ELBO(t) - O(eps_mixsqp * L * M * S_m)},
Neal & Hinton 1998). \code{"elbo"} stops when the change in
ELBO falls below \code{tol}; the per-iteration ELBO is a
coherent variational free energy thanks to the
\code{get_objective.mfsusie} post-iteration KL refresh
(\code{refresh_lbf_kl.mf_individual}) which evaluates per-
effect \code{KL[l]} against the iter-final pi_V rather than the
pi_V state at the moment effect l was updated.}

\item{pip_stall_window}{integer. Number of consecutive
iterations without PIP-difference improvement after which
the PIP-based convergence path declares convergence even
when \code{tol} has not been reached. Default 5. Only consulted
when \code{convergence_method = "pip"} (or when ELBO produces
non-finite values and the PIP fallback fires).}

\item{estimate_residual_variance}{logical. When \code{TRUE}
(default), update \code{sigma2} per IBSS iteration via
\code{update_variance_components}. When \code{FALSE}, hold \code{sigma2}
at the initial value across iterations; useful for
sensitivity analysis.}

\item{verbose}{logical. Default \code{TRUE}; set to \code{FALSE} for
silent runs.}

\item{track_fit}{logical, retain a per-iteration tracking list on
the fit. Default \code{FALSE}.}

\item{max_padded_log2}{integer, log2 cap on the post-remap grid
length per outcome. Default 10.}

\item{wavelet_basis_order}{integer; selects the wavelet basis
member within \code{wavelet_family}. For Daubechies families this
equals the number of vanishing moments (higher = smoother,
longer-support filter). Forwarded to \code{wavethresh::wd}'s
\code{filter.number}; see also \code{filter.select}. Default \code{10}.}

\item{wavelet_family}{character; selects the wavelet family.
Forwarded to \code{wavethresh::wd}'s \code{family}. Default
\code{"DaubLeAsymm"} (Daubechies least-asymmetric, a.k.a. Symmlet).}

\item{wavelet_magnitude_cutoff}{non-negative numeric. After
the wavelet decomposition, each wavelet column \code{t} of the
response has its median absolute value \verb{median(|Y_wd[, t]|)}
compared against this cutoff; columns at or below are zeroed
in the data and treated as \code{Bhat = 0}, \code{Shat = 1} at every
IBSS iteration. Useful for sparse-coverage assays where
high-frequency wavelet coefficients are dominated by zeros.
Default \code{0}; only columns with a strictly-zero absolute-value
median are masked.}

\item{wavelet_qnorm}{logical. When \code{TRUE} (default), applies a
column-wise rank-based normal quantile transform to the
wavelet-domain response before the IBSS loop. Useful for
non-Gaussian wavelet coefficients arising from heavy-tailed
assays.}

\item{control_mixsqp}{optional named list of \code{mixsqp} control
arguments forwarded to the per-(outcome, scale) M-step.}

\item{mixture_null_weight}{numeric in \verb{[0, 1]}. Ratio of null
pseudo-weight to total data weight in the mixsqp M-step,
regularizing the EB scale-mixture prior toward the null
component. \code{0} is unregularized MLE; \code{0.05} (default) is
light shrinkage toward null. Internally scaled by the number
of outcomes \code{M} so the null:data balance is invariant to
\code{M}; single-outcome fits are unchanged.}

\item{mixsqp_alpha_eps}{numeric, threshold below which a
variable's per-effect posterior \code{alpha[l, j]} is dropped
from the mixsqp M-step input. The truncation error on the
M-step gradient is bounded by \verb{sum_\{j outside\} alpha_j * max_k(L_jk)}, well under floating-point precision for
typical concentrated SuSiE posteriors. Set to \code{0} to use
every variable regardless of \code{alpha[l, j]}. Default \code{1e-6}.}

\item{model_init}{optional \code{mfsusie} fit object from a prior
call. When supplied, the IBSS loop is seeded from the
supplied \code{alpha}, \code{mu}, \code{mu2}, \code{KL}, \code{lbf}, \code{V}, \code{pi_V},
\code{sigma2}, \code{fitted}, and \code{intercept} rather than the cold-
start zero state. The supplied fit must have the same \code{L}
as the requested \code{L}; otherwise the call errors. Useful
for resuming a long-running fit after a per-call iteration
budget. Default \code{NULL} (cold start).}

\item{small_sample_correction}{logical. When \code{TRUE}, replaces
the per-variable Wakefield Normal marginal Bayes factor in
the SER step with a Johnson 2005 scaled Student-t marginal
with \code{df = n - 1} degrees of freedom. Useful for small
sample sizes where the Wakefield approximation
under-propagates residual-variance uncertainty into the
per-variable BF, inflating PIPs at null variants. The
correction acts on variable selection probabilities only;
posterior moments given inclusion are unchanged. Default
\code{FALSE}.}

\item{attach_smoothing_inputs}{logical. When \code{TRUE} (default),
the fit carries \code{Y_grid} (post-remap position-space Y) and
\code{X_eff} (per-effect alpha-weighted aggregate of X) so that
\code{mf_post_smooth(fit)} runs without re-passing the data.
Set \code{FALSE} to drop these and call
\code{mf_post_smooth(fit, X = X, Y = Y, ...)} instead; useful
when sharing fits where the per-individual data should
not travel with the fit.}
}
\value{
A list of class \code{c("mfsusie", "susie")} carrying:
\describe{
\item{\code{alpha}}{\verb{L x p} posterior inclusion probabilities.}
\item{\code{mu}, \code{mu2}}{\code{list[L]} of \code{list[M]} of \verb{p x T_basis[m]}
matrices: per-effect, per-outcome posterior mean and second
moment in the wavelet domain.}
\item{\code{pi_V}}{\code{list[M]} of \verb{S_m x K} mixture-weight matrices.}
\item{\code{G_prior}}{\code{list[M]} of \code{list[S_m]} ash-shaped prior
records (mutated by the IBSS loop).}
\item{\code{sigma2}}{\code{list[M]}. Element m is either a scalar
(when \code{residual_variance_scope = "per_outcome"}, the
default) or a length-\code{S_m} vector of per-scale residual
variances (when \code{residual_variance_scope = "per_scale"}).
The richer per-scale shape is mfsusieR-specific;
downstream code reading \code{sigma2} should handle both.}
\item{\code{elbo}}{numeric vector of ELBO values per iteration.}
\item{\code{niter}, \code{converged}}{IBSS termination metadata.}
\item{\code{pip}}{length-p posterior inclusion probabilities.}
\item{\code{sets}}{credible sets via \code{susie_get_cs}.}
\item{\code{fitted}}{\code{list[M]} of running per-outcome fits in the
wavelet domain. Used by \code{model_init} to warm-start a
follow-up call.}
\item{\code{Y_grid}}{\code{list[M]}, the post-remap position-space
response on the padded grid. Attached when
\code{attach_smoothing_inputs = TRUE} (default).}
\item{\code{X_eff}}{\code{list[L]} of per-effect alpha-weighted X
aggregates. Attached when \code{attach_smoothing_inputs = TRUE}.}
\item{\code{lbf_variable_outcome}}{\verb{L x p x M} array of per-(effect, variant,
outcome) log Bayes factors. Parallels \code{lbf_variable} (which is
\verb{L x p}, the joint composite summed across scales and outcomes);
\code{lbf_variable_outcome} keeps the M axis intact. Always attached;
consumed by \code{susie_post_outcome_configuration(fit, by = "outcome")}.}
}
}
\description{
Fit the multi-functional, multi-outcome Sum of Single Effects
(mfSuSiE) regression model. Each outcome \code{m} carries a per-sample
functional response \code{Y_m} of length \code{T_m}; \code{T_m} may differ across
outcomes, and either \code{T_m = 1} (univariate response) or \code{T_m > 1}
(functional response on a regular grid) is supported; for the latter
mfsusieR runs a per-outcome wavelet decomposition, then dispatches into susieR's
IBSS workhorse with mfsusieR-specific S3 methods that handle the
per-(scale, outcome) mixture-of-normals prior and residual
variance.
}
\references{
Manuscript: methods/online_method.tex (mfsusieR algorithm).
}
