% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfsusie_plot.R
\name{mfsusie_plot_lfsr_dimensions}
\alias{mfsusie_plot_lfsr_dimensions}
\title{Recommended figure dimensions for \code{mfsusie_plot_lfsr()}}
\usage{
mfsusie_plot_lfsr_dimensions(fit, add_legend = TRUE)
}
\arguments{
\item{fit}{an \code{mfsusie} / \code{fsusie()} fit object that has been
post-processed with \code{mf_post_smooth(method = "HMM")}.}

\item{add_legend}{logical, whether the lfsr legend strip is
shown (matches the corresponding \code{mfsusie_plot_lfsr()} arg).
Default \code{TRUE}.}
}
\value{
a list with components \code{width}, \code{height} (numeric, in
inches), \code{cols}, \code{rows} (integer, the M-grid layout used).
}
\description{
Returns a \verb{(width, height)} (in inches) sized to the per-CS
bubble grid so each bubble row has enough vertical space and
each outcome panel has enough horizontal space. Mirrors
\code{mfsusie_plot_dimensions()} for the lfsr companion plot. Use
the result for \code{fig.width} / \code{fig.height} in a knitr chunk so
the legend and bubbles do not collide.
}
