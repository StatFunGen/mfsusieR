% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_mu_method.R
\name{mf_thin}
\alias{mf_thin}
\title{Thin an \code{mfsusie} fit by replacing per-variant posterior moments}
\usage{
mf_thin(fit, method = c("alpha_collapsed", "lead"))
}
\arguments{
\item{fit}{an \code{mfsusie} fit object. Must currently be in \code{"complete"}
storage; thinning a fit that is already 1D is rejected.}

\item{method}{one of \code{"alpha_collapsed"} or \code{"lead"}. See \code{mfsusie()}'s
\code{save_mu_method} documentation for the per-mode semantics.}
}
\value{
a new \code{mfsusie} fit with the chosen storage shape. The original
\code{fit} is not modified.
}
\description{
Applies the same storage trim as \code{mfsusie(save_mu_method = method)} but
as a post-fit operation, returning a new fit object. Lets callers keep a
complete fit for warm-starts and a thinned copy for distribution / saving.
}
\details{
Both 1D modes drop information that the SER-step IBSS update
needs, so the returned fit cannot be passed back as \code{model_init}. Use
the thinned copy for \code{coef}, \code{mf_post_smooth}, \code{summary}, \code{print}, and
alpha-aggregated plots; keep the complete fit for \code{predict(newx)},
per-variant lfsr, and warm-start checkpoints.
}
\examples{
\dontrun{
  fit_full <- mfsusie(X, Y)                              # complete
  saveRDS(fit_full, "checkpoint.rds")                    # warm-start ready
  fit_thin <- mf_thin(fit_full, method = "alpha_collapsed")
  saveRDS(fit_thin, "for_distribution.rds")              # ~factor p smaller
}
}
