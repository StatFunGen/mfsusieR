% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{mf_simu_ibss_per_level}
\alias{mf_simu_ibss_per_level}
\title{Sample a smooth random function from the IBSS wavelet prior}
\usage{
mf_simu_ibss_per_level(
  lev_res = 7L,
  length_grid = 10L,
  pi0,
  alpha = 0.8,
  prop_decay = 0.1
)
}
\arguments{
\item{lev_res}{integer \verb{>= 2}. Resolution level: the
sampled function lives on a length-\code{2^lev_res} grid.
Default \code{7} (length 128).}

\item{length_grid}{integer \verb{>= 2}. Number of components in
the per-scale ash mixture, including the null component
at zero. Default \code{10}.}

\item{pi0}{optional numeric vector of length \code{lev_res}.
Per-scale null-component weight. When omitted, defaults
to \code{1 - exp(-prop_decay * 1:lev_res)}.}

\item{alpha}{non-negative numeric. Scale-dependent variance
shrinkage exponent: at scale \code{s}, the per-component sd is
divided by \code{2^{alpha * s}}. Larger \code{alpha} produces
smoother functions. Default \code{0.8}.}

\item{prop_decay}{numeric in \verb{[0, 1]}. Controls how the
default \code{pi0} decays across scales: larger \code{prop_decay}
gives a sparser fine-scale signal. Ignored when \code{pi0} is
supplied explicitly. Default \code{0.1}.}
}
\value{
A list with components
\describe{
\item{\code{sim_func}}{numeric vector of length \code{2^lev_res},
the position-space sample.}
\item{\code{true_coef}}{numeric vector of the wavelet detail
coefficients used to construct the sample.}
\item{\code{mix_per_scale}}{length-\code{lev_res} list of
\code{ashr::normalmix} objects, one per wavelet scale.}
\item{\code{emp_pi0}}{length-\code{lev_res} numeric vector of
empirical null-component fractions per scale (the
fraction of detail coefficients that came back exactly
zero from the mixture sampler).}
}
}
\description{
Draws a length-\code{2^lev_res} function whose wavelet detail
coefficients at scale \code{s} are independent draws from an
ash-style normal mixture
\verb{\\sum_k \\pi_\{s,k\} N(0, (\\sigma_k / 2^\{\\alpha s\})^2)}.
The first mixture component is a point mass at zero with
weight \code{pi0[s]}; the remaining components share a common
grid of standard deviations on \verb{(0, \\infty)}. The inverse
DWT (\code{wavethresh::wr}) maps the sampled coefficient vector
to position space.
}
\details{
This is the prior class fSuSiE infers in its IBSS step, so
the bundled simulation matches the model's assumed
generative process. Used by the example datasets and the
covariate-adjustment vignette.
}
\examples{
set.seed(1L)
f <- mf_simu_ibss_per_level(lev_res = 7L)
length(f$sim_func)            # 128
plot(f$sim_func, type = "l", xlab = "position", ylab = "f")

}
\references{
Manuscript: methods/derivation.tex (IBSS prior derivation).
}
