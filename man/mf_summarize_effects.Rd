% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mfsusie_methods.R
\name{mf_summarize_effects}
\alias{mf_summarize_effects}
\title{Per-(CS, outcome) regions where the credible band excludes zero}
\usage{
mf_summarize_effects(fit, smooth_method = NULL)
}
\arguments{
\item{fit}{an \code{mfsusie} / \code{fsusie()} fit. The fit must
carry post-smoothed credible bands; call
\code{mf_post_smooth(fit, method = ...)} first.}

\item{smooth_method}{optional name of the smoother to read
from \code{fit$smoothed}. When \code{NULL} (default) the smoother
priority order is used: \verb{"TI" > "smash" > "HMM" > "scalewise"}.}
}
\value{
A data frame with columns
\describe{
\item{\code{cs_index}}{integer, the credible-set index
(1..length(fit$sets$cs_index)).}
\item{\code{outcome}}{integer, the outcome index \code{m}.}
\item{\code{start}, \code{end}}{integer position indices
defining the contiguous run on the
\code{T_basis[m]}-position grid where the band excludes
zero.}
\item{\code{n_positions}}{integer, run length
(\code{end - start + 1}).}
}
When no CS has any band-excludes-zero run, returns an
empty data frame with the same columns.
}
\description{
Given an \code{mfsusie} fit that has been post-smoothed via
\code{mf_post_smooth()}, returns one row per \verb{(CS, outcome, contiguous-run)} triple identifying the position ranges
where the smoothed credible band excludes zero. Useful as a
compact diagnostic for "where on the curve does this CS act
on this outcome?".
}
\examples{
\donttest{
set.seed(1L)
n <- 100; p <- 20; T_m <- 32L
X <- matrix(rnorm(n * p), n)
beta <- numeric(p); beta[3] <- 1.2
shape <- exp(-((seq_len(T_m) - T_m / 2)^2) / (2 * 6^2))
Y <- X \%*\% (matrix(beta, p, 1) \%*\% matrix(shape, 1, T_m)) +
       matrix(rnorm(n * T_m, sd = 0.3), n)
fit <- fsusie(Y, X, L = 1, verbose = FALSE)
fit_s <- mf_post_smooth(fit, method = "TI")
mf_summarize_effects(fit_s)
}
}
